<table class="ui table">
  <thead>
    <tr>
      <th>Song</th>
      <th>Artist</th>
          <a href="/clublist/{{../clublist.id}}/deletesong/{{id}}" class="ui icon button">
  <i class="icon trash"></i>
</a>
    </tr>
  </thead>
  <tbody>
    {{#each playlist.songs}}
      <tr>
        <td>
          {{title}}
        </td>
        <td>
          {{artist}}
        </td>
      </tr>
    {{/each}}
  </tbody>
</table>
